# Pin the toolchain so a local build and CI agree.
#
# Without this, `cargo clippy` locally used whatever rustc happened to be
# installed while CI used the current stable — and clippy gains lints between
# releases, so "clean locally" quietly stopped meaning "clean in CI".
[toolchain]
channel = "1.98.0"
components = ["clippy", "rustfmt"]
