{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions the Micro Card window needs.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "os:default",
    "dialog:allow-open",
    "dialog:allow-message",
    "dialog:allow-confirm",
    "opener:allow-open-url",
    "notification:default",
    "dialog:allow-save"
  ]
}
